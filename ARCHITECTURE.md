# PLONK Demo 完整架構說明

## 🎯 **您在 Demo 什麼？**

**核心目標**：展示完整的 PLONK 零知識證明協議，證明「我知道一個秘密數字 w，使得 w² + x = y」，但**不洩露 w 的值**。

### **具體例子**
- **秘密 witness**：`w = 3`（只有 Prover 知道）
- **公開輸入**：`x = 3`, `y = 12`（大家都知道）
- **證明目標**：向 Verifier 證明「我知道某個 w 使得 w² + 3 = 12」，但不告訴你 w 是 3
- **驗證**：`3² + 3 = 9 + 3 = 12` ✓（正確，但 Verifier 不知道 w = 3）

---

## 📐 **PLONK 協議完整流程**

### **四大階段**

```
┌─────────────────────────────────────────────────────────────────┐
│                    PLONK 協議完整流程                             │
└─────────────────────────────────────────────────────────────────┘

  階段 1: Universal Setup (第 6 章)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [Trusted Setup] → 生成 SRS (Structured Reference String)

  輸入: 安全參數 n = 16 (domain 大小)
  輸出: PublicParameters (SRS)
       - G₁ 元素: [1]₁, [x]₁, [x²]₁, ..., [x¹⁶]₁
       - G₂ 元素: [1]₂, [x]₂

  特點: ✅ 只需執行一次，可重複使用
       ✅ 任何大小 ≤ 16 的電路都能用

  ↓

  階段 2: Circuit Definition & Preprocessing (第 7 章)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [Circuit Builder] → 定義電路 w² + x = y

  電路結構:
    Gate 0 (乘法): w × w = t      (t 是中間變數)
    Gate 1 (加法): t + x = y

  Preprocessing 生成:
    - Selector 多項式承諾: [q_L]₁, [q_R]₁, [q_M]₁, [q_O]₁, [q_C]₁
    - Permutation 多項式承諾: [S_σ1]₁, [S_σ2]₁, [S_σ3]₁

  輸出:
    - Prover (持有預處理數據)
    - Verifier (持有驗證密鑰)

  ↓

  階段 3: Prover 證明生成 (第 8 章 - 5 Rounds)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [Prover] → 生成零知識證明

  輸入:
    - 秘密 witness: w = 3
    - 公開輸入: x = 3, y = 12
    - 預處理數據 (selectors, permutations)

  Round 1: Wire Polynomials
    ├─ 構造 wire 多項式 a(X), b(X), c(X)
    ├─ 對應 witness 賦值: [w, t], [w, x], [t, y]
    └─ 生成承諾: [a]₁, [b]₁, [c]₁

  Round 2: Permutation Argument
    ├─ 接收挑戰: β, γ (來自 Fiat-Shamir)
    ├─ 計算累積多項式 z(X)
    └─ 生成承諾: [z]₁

  Round 3: Quotient Polynomial
    ├─ 接收挑戰: α
    ├─ 計算商多項式 t(X) = t_lo + X^n·t_mid + X^(2n)·t_hi
    └─ 生成承諾: [t_lo]₁, [t_mid]₁, [t_hi]₁

  Round 4: Opening Evaluations
    ├─ 接收挑戰: ζ (評估點)
    └─ 計算評估值: a(ζ), b(ζ), c(ζ), s_σ1(ζ), s_σ2(ζ), z(ωζ)

  Round 5: Opening Proofs
    ├─ 接收挑戰: v, u
    ├─ 計算批量開啟證明
    └─ 生成: [W_ζ]₁, [W_ζω]₁

  輸出: Proof (約 7 個橢圓曲線點 + 6 個域元素)
       大小約 ~500 bytes

  複雜度:
    - FFT 次數: ~21 次
    - MSM 次數: ~14 次

  ↓

  階段 4: Verifier 驗證 (第 9 章 - 6 Steps)
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  [Verifier] → 驗證證明有效性

  輸入:
    - Proof (來自 Prover)
    - 公開輸入: x = 3, y = 12
    - 驗證密鑰 (來自預處理)

  Step 1: 重建 Fiat-Shamir Transcript
    └─ 用相同方式重新計算挑戰值: β, γ, α, ζ, v, u

  Step 2: 評估 Public Input 多項式
    └─ 計算 PI(ζ) = L₁(ζ)·x + L₂(ζ)·y

  Step 3: 計算 Vanishing Polynomial
    └─ Z_H(ζ) = ζ^n - 1

  Step 4: 計算 Lagrange Basis
    └─ L₁(ζ) = (ζ^n - 1) / (n·(ζ - 1))

  Step 5: 計算線性化多項式
    └─ r(ζ) = 組合所有約束檢查

  Step 6: 批量多項式開啟驗證
    └─ 使用 2 個 Pairing 檢查:
       e([W_ζ]₁ + u·[W_ζω]₁, [x]₂) ?= e([...], [1]₂)

  輸出: ✅ Accept / ❌ Reject

  複雜度:
    - 橢圓曲線運算: O(1) (常數時間)
    - Pairing 運算: 2 次
    - 驗證時間: ~10ms (非常快！)
```

---

## 🔧 **技術實作架構**

### **Backend (Rust)**

```
backend/
├── src/
│   ├── main.rs           ━━━ 主協調器，串聯所有階段
│   │   └─ 流程: Setup → Circuit → Prove → Verify
│   │   └─ 輸出: 5 個 JSON 檔案到 web/data/
│   │
│   ├── types.rs          ━━━ JSON 資料結構定義
│   │   ├─ SetupMetrics        (Setup 統計)
│   │   ├─ PreprocessMetrics   (預處理統計)
│   │   ├─ CircuitInfo          (電路資訊)
│   │   ├─ ProverTrace          (Prover 軌跡)
│   │   └─ VerifierTrace        (Verifier 軌跡)
│   │
│   ├── srs.rs            ━━━ 第 6 章：Universal Setup
│   │   └─ run_setup(n) → (PublicParameters, SetupMetrics)
│   │
│   ├── circuit.rs        ━━━ 第 7 章：電路定義
│   │   ├─ SquareAddCircuit   結構
│   │   │   ├─ w: BlsScalar    (witness)
│   │   │   ├─ x: BlsScalar    (public)
│   │   │   └─ y: BlsScalar    (public)
│   │   │
│   │   └─ impl Circuit trait
│   │       └─ circuit(&self, composer) {
│   │            Gate 0: t = composer.gate_mul(w * w)
│   │            Gate 1: sum = composer.gate_add(t + x)
│   │            約束: composer.assert_equal(sum, y)
│   │          }
│   │
│   ├── prover.rs         ━━━ 第 8 章：Prover 5 Rounds
│   │   └─ prove_with_trace(prover)
│   │       ├─ Round 1: Wire Polynomials
│   │       ├─ Round 2: Permutation
│   │       ├─ Round 3: Quotient
│   │       ├─ Round 4: Evaluations
│   │       ├─ Round 5: Openings
│   │       └─ 返回: (Proof, ProverTrace)
│   │
│   └── verifier.rs       ━━━ 第 9 章：Verifier 驗證
│       └─ verify_with_trace(verifier, proof, public_inputs)
│           ├─ Step 1: Rebuild Transcript
│           ├─ Step 2: Evaluate PI(ζ)
│           ├─ Step 3: Compute Z_H(ζ)
│           ├─ Step 4: Compute L₁(ζ)
│           ├─ Step 5: Linearization
│           ├─ Step 6: Batch Verification
│           └─ 返回: (bool, VerifierTrace)
│
└── Cargo.toml            ━━━ 依賴配置
    └─ dusk-plonk = "0.19"
```

### **Frontend (Web)**

```
web/
├── data/                       ━━━ Backend 生成的 JSON 數據
│   ├── setup_metrics.json        (Setup 統計)
│   ├── preprocess_metrics.json   (預處理統計)
│   ├── circuit_info.json         (電路詳情)
│   ├── prover_trace.json         (5 rounds 軌跡)
│   ├── verifier_trace.json       (6 steps 軌跡)
│   └── summary.txt               (執行摘要)
│
├── demo.js                    ━━━ JavaScript 載入和展示數據
│   ├─ loadSetup()              → 展示 Setup 統計
│   ├─ loadCircuit()            → 展示電路結構和 gates
│   ├─ loadProver()             → 展示 5 rounds 詳情
│   └─ loadVerifier()           → 展示 6 steps 驗證流程
│
└── demo_sections.html         ━━━ HTML 模板（待整合到 index.html）
    ├─ CSS 樣式
    ├─ #ch6-demo  (第 6 章 demo 區塊)
    ├─ #ch7-demo  (第 7 章 demo 區塊)
    ├─ #ch8-demo  (第 8 章 demo 區塊)
    └─ #ch9-demo  (第 9 章 demo 區塊)
```

---

## 🎭 **數據流程**

```
┌─────────────┐
│   Backend   │
│   (Rust)    │
└──────┬──────┘
       │
       │ 執行 cargo run --release
       │
       ├─────────────────────────────────────────────┐
       │                                             │
       ▼                                             ▼
  Step 1: Setup                              Step 2: Circuit
  生成 SRS                                   定義 w²+x=y
  ↓                                          ↓
  setup_metrics.json                         circuit_info.json
                                             preprocess_metrics.json
       │                                             │
       ├─────────────────────────────────────────────┤
       │                                             │
       ▼                                             ▼
  Step 3: Prover                             Step 4: Verifier
  5 rounds 生成證明                          6 steps 驗證證明
  ↓                                          ↓
  prover_trace.json                          verifier_trace.json
                                             summary.txt
       │
       │ 所有 JSON 寫入 web/data/
       │
       ▼
┌─────────────┐
│   Frontend  │
│  (Browser)  │
└──────┬──────┘
       │
       │ 開啟 index.html
       │
       ├─ demo.js 自動執行
       │
       ├─ fetch('data/setup_metrics.json')     → 顯示在第 6 章
       ├─ fetch('data/circuit_info.json')      → 顯示在第 7 章
       ├─ fetch('data/prover_trace.json')      → 顯示在第 8 章
       └─ fetch('data/verifier_trace.json')    → 顯示在第 9 章

       ▼
  使用者看到完整的 PLONK 流程展示
  - 每個階段的統計數據
  - 電路結構和 gate 詳情
  - Prover 5 rounds 的執行軌跡
  - Verifier 6 steps 的驗證過程
```

---

## 🎓 **教育價值**

### **這個 Demo 展示了什麼？**

1. **零知識性**：
   - Prover 證明「我知道 w 使得 w²+3=12」
   - 但**從不洩露 w = 3**
   - Verifier 無法從 proof 反推出 w

2. **簡潔性**：
   - 證明大小：~500 bytes（固定）
   - 驗證時間：~10ms（常數時間）
   - 無論電路多複雜，證明都很小

3. **Universal Setup**：
   - Setup 一次，適用所有 ≤16 gates 的電路
   - 可更新（Updatable）
   - 不需要每個電路重新 setup

4. **完整流程**：
   - 從數學理論到實際代碼
   - 每個階段的計算細節
   - FFT/MSM 複雜度分析

---

## 🔍 **與報告 (index.html) 的對應**

```
index.html (理論說明)          Backend (實際執行)         Frontend Demo
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 6 章: Universal Setup      srs.rs                   #ch6-demo
- 理論：SRS 生成原理          - run_setup(16)          - 顯示 Setup 統計
- 數學：τ powers              - G1: 17, G2: 2          - Domain 大小
                                                       - SRS 大小

第 7 章: 電路與約束系統        circuit.rs               #ch7-demo
- 理論：Gate 定義             - SquareAddCircuit       - 顯示 2 個 gates
- 數學：Constraint System     - gate_mul, gate_add     - Wire 賦值
                              - Preprocessing          - Selector 值

第 8 章: Prover 協議          prover.rs                #ch8-demo
- 理論：5 rounds 說明         - prove_with_trace()     - 顯示每 round
- 數學：多項式計算            - 記錄 FFT/MSM           - 承諾、挑戰
                              - 追蹤時間               - 複雜度統計

第 9 章: Verifier 驗證        verifier.rs              #ch9-demo
- 理論：6 steps 說明          - verify_with_trace()    - 顯示每 step
- 數學：Pairing 檢查          - 計算 PI(ζ), Z_H(ζ)    - 方程式
                              - Batch verification     - 驗證結果
```

---

## ✨ **最終成果**

用戶打開 `index.html`，會看到：

1. **完整的理論說明**（已存在的報告內容）
2. **實際執行的 Demo**（新增的互動式展示）
   - 第 6 章末尾：Setup 統計數據
   - 第 7 章末尾：電路結構和 gates
   - 第 8 章末尾：Prover 5 rounds 執行軌跡
   - 第 9 章末尾：Verifier 6 steps 驗證流程

**理論 + 實踐完美結合！**

---

## 📝 **總結**

**您在 Demo 的核心**：
- 一個**可運行的 PLONK 零知識證明系統**
- 證明「我知道秘密 w=3 使得 w²+3=12」
- 完整展示從 Setup → Circuit → Prove → Verify 的全流程
- 理論報告 + 實際執行數據的教育展示

**技術亮點**：
- ✅ 使用業界標準庫 dusk-plonk
- ✅ 真實的橢圓曲線運算（BLS12-381）
- ✅ 完整的數據追蹤和可視化
- ✅ 可重現、可擴展的架構

**適用場景**：
- 🎓 教學展示
- 📊 研究驗證
- 💡 原型開發
- 🔍 協議理解
